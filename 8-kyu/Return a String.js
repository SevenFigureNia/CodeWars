// Create a function that accepts a parameter representing a name and returns the message: "Hello, <name> how are you doing today?".

// [Make sure you type the exact thing I wrote or the program may not execute properly]
function greet(name) {
  return `Hello, ${name} how are you doing today?`;
}
// I finally got to the answer! After working through the function structure, I realized I needed to use a template literal with ${name} so the name could be inserted into the message correctly. Small detail, but it made the whole Codewars challenge click. 💻✨

